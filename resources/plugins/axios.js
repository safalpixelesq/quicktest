export default function ({ $axios, store }) {
	// $axios.onRequest((config) => {
	// 	// config.baseURL = process.env.API_URL || 'https://api.uiaas.io'
	// })
	// $axios.onError((error) => {
	// 	throw error.response
	// })
}
